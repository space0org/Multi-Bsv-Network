version: '3'

services:
  # JpyNetwork Node
  jpynetwork-node:
    image: bitcoinsv/bitcoin-sv:1.0.8.beta
    container_name: jpynetwork-node
    volumes:
      - ./jpynetwork/data:/home/bitcoin/.bitcoin
      - ./jpynetwork/bitcoin.conf:/home/bitcoin/.bitcoin/bitcoin.conf
    ports:
      - "18332:18332"
      - "18333:18333"
      - "18444:18444"
    networks:
      - bsv-network

  # LariNetwork Node
  larinetwork-node:
    image: bitcoinsv/bitcoin-sv:1.0.8.beta
    container_name: larinetwork-node
    volumes:
      - ./larinetwork/data:/home/bitcoin/.bitcoin
      - ./larinetwork/bitcoin.conf:/home/bitcoin/.bitcoin/bitcoin.conf
    ports:
      - "19332:18332"
      - "19333:18333"
      - "19444:18444"
    networks:
      - bsv-network

  # Token Bridge
  token-bridge:
    build: ./token-bridge
    container_name: token-bridge
    ports:
      - "5001:5001"
    networks:
      - bsv-network
    depends_on:
      - jpynetwork-node
      - larinetwork-node

networks:
  bsv-network:
    driver: bridge
